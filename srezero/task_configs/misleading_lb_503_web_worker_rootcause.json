{
  "task_id": "misleading_lb_503_web_worker_rootcause",
  "difficulty": "hard",
  "alert": "The load balancer is returning intermittent 503s.",
  "root_cause": "web worker saturation causing load balancer 503s",
  "root_cause_keywords": ["web", "worker", "saturation"],
  "relevant_evidence": [
    "inspect_logs:load_balancer",
    "inspect_metrics:web_server",
    "inspect_config:web_server:MAX_WORKERS"
  ],
  "evidence_descriptions": {
    "inspect_logs:load_balancer": "Load balancer logs show no available web backend connections.",
    "inspect_metrics:web_server": "Web metrics show worker utilization at capacity.",
    "inspect_config:web_server:MAX_WORKERS": "MAX_WORKERS is too low for current traffic."
  },
  "correct_fix": {
    "action_type": "update_config",
    "service": "web_server",
    "key": "MAX_WORKERS",
    "min_numeric_value": 32,
    "fix_keywords": ["web", "workers"]
  },
  "service_patches": {
    "load_balancer": {
      "logs": [
        "ERROR route=/api/search status=503 reason=no_available_backend_connection",
        "WARN backend=web_server queue_wait_ms=2100",
        "INFO health_check backend=web_server healthy=true"
      ],
      "metrics": {
        "backend_5xx_rate": 0.17,
        "healthy_backends": 2,
        "connection_utilization_pct": 45
      }
    },
    "web_server": {
      "logs": [
        "WARN worker queue depth=880 max_workers=16",
        "WARN request queued wait_ms=1900",
        "INFO database latency_ms=25"
      ],
      "metrics": {
        "worker_utilization_pct": 99,
        "queued_requests": 880,
        "p95_latency_ms": 2400
      },
      "config": {
        "MAX_WORKERS": 16
      }
    }
  },
  "expected_action_pattern": [
    "inspect_logs(load_balancer)",
    "inspect_metrics(web_server)",
    "inspect_config(web_server, MAX_WORKERS)",
    "update_config(web_server, MAX_WORKERS, 32)",
    "resolve_incident(web worker saturation causing load balancer 503s, increase web workers)"
  ],
  "distractors": [
    "The load balancer is reporting symptoms; backend web worker capacity is exhausted."
  ],
  "distractor_services": ["load_balancer"],
  "max_steps": 10,
  "metadata": {
    "source": "config"
  }
}

